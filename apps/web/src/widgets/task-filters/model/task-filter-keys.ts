export type TaskFilterKey = "priority" | "assignee" | "author" | "tags" | "date";

export const taskFilterKeys: TaskFilterKey[] = ["priority", "assignee", "author", "tags", "date"];
export const maxVisibleTaskFilters = 3;
